Ejemplos para el repositorio (en el servidor se copian bajo /etc/zabbix/zabbix_agent2.d/
según tu paquete).

plugins.d/mysql.conf.example
  Fragmentos para el plugin MySQL del Zabbix agent 2 (Plugins.Mysql.*).
  Copiar/adaptar a .../zabbix_agent2.d/plugins.d/mysql.conf (o como indique tu distro).
